# 最长公共子序列 (LCS) 算法

## 1 题目

请使用C++编程实现最长公共子序列（LCS）算法：时间复杂度O(mn)，空间复杂度O(mn)，求出LCS及其长度；时间复杂度O(mn)，空间复杂度O(2\*min(m,n))，求出LCS的长度；时间复杂度O(mn)，空间复杂度O(min(m,n))，求出LCS的长度。给定两个字符串 text1 和 text2，返回这两个字符串的最长公共子序列的长度，一个字符串的 子序列 是指这样一个新的字符串：它是由原字符串在不改变字符的相对顺序的情况下删除某些字符（也可以不删除任何字符）后组成的新字符串。例如，"ace" 是 "abcde" 的子序列，但 "aec" 不是 "abcde" 的子序列。两个字符串的「公共子序列」是这两个字符串所共同拥有的子序列。若这两个字符串没有公共子序列，则返回 0。由控制台输入两个字符串text1，text2。输入的字符串只含有小写英文字符（a~z）；输出控制台打印LCS的长度及相应的序列，如不存在公共子序列则返回0.

## 2 核心算法说明

### 什么是最长公共子序列 (LCS)

一个字符串的**子序列**是指从原字符串中删除某些字符（也可以不删除）后，不改变剩余字符相对顺序得到的新字符串。

例如：

- `"ace"` 是 `"abcde"` 的子序列
- `"aec"` 不是 `"abcde"` 的子序列（顺序不对）

**最长公共子序列**就是两个字符串共同拥有的最长子序列。

### 算法核心思想 - 动态规划

设 `dp[i][j]` 表示 `text1[0..i-1]` 和 `text2[0..j-1]` 的最长公共子序列长度。

**状态转移方程**：

```
如果 text1[i-1] == text2[j-1]:
    dp[i][j] = dp[i-1][j-1] + 1
否则:
    dp[i][j] = max(dp[i-1][j], dp[i][j-1])
```

### 三种实现的空间优化

| 方法   | 空间复杂度    | 实现方式          | 能否回溯LCS |
| ------ | ------------- | ----------------- | ----------- |
| 方法一 | O(mn)         | 完整DP表          | ✓ 可以      |
| 方法二 | O(2×min(m,n)) | 两行滚动数组      | ✗ 不可以    |
| 方法三 | O(min(m,n))   | 单行数组+临时变量 | ✗ 不可以    |

**方法一**：保存完整的DP表，可以通过回溯找出具体的LCS字符串。

**方法二**：观察到计算 `dp[i][j]` 只需要 `dp[i-1][j]`、`dp[i][j-1]` 和 `dp[i-1][j-1]`，因此只需保存两行。

**方法三**：进一步优化，只用一行数组，用一个临时变量保存 `dp[i-1][j-1]` 的值。

## 3 如何运行

### 交互式运行（推荐）

由于程序需要控制台输入，请使用以下命令启动交互模式：

```bash
# 构建并启动交互式容器
docker-compose run --rm backend
```

启动后，程序会等待输入。请输入两个字符串（例如 `abcde ace`）并回车。

### 自动化构建（仅构建）

```bash
docker-compose up --build -d
```

### 运行示例输出

```
================================================
   最长公共子序列 (LCS) 算法演示
================================================

使用内置测试用例进行演示...

共 5 组测试用例

########################################
测试用例 1/5
########################################

输入信息：
text1 = "abcde" (长度: 5)
text2 = "ace" (长度: 3)

========================================
方法一：空间复杂度 O(m*n)
========================================
特点：可以求出LCS字符串及其长度
LCS长度：3
LCS序列："ace"

========================================
方法二：空间复杂度 O(2*min(m,n))
========================================
特点：使用两行滚动数组，仅求LCS长度
LCS长度：3

========================================
方法三：空间复杂度 O(min(m,n))
========================================
特点：使用单行数组，仅求LCS长度
LCS长度：3

========================================
结果验证
========================================
[OK] 三种方法计算结果一致，LCS长度均为：3

... (更多测试用例) ...

================================================
全部测试用例执行完毕！
================================================
```

### 本地编译运行（无 Docker）

```bash
cd backend
g++ -o app main.cpp -std=c++11 -O2

# 使用内置测试用例
./app

# 或指定输入
./app abcde ace
```

## 4 目录结构说明

```
.
├── backend/                 # 核心代码目录
│   ├── main.cpp            # LCS算法C++实现（三种空间复杂度）
│   └── Dockerfile          # Docker构建配置
├── docker-compose.yml      # Docker Compose配置
├── .gitignore              # Git忽略文件配置
└── README.md               # 项目说明文档
```

## 5 测试用例

| text1      | text2       | LCS长度 | LCS序列     |
| ---------- | ----------- | ------- | ----------- |
| abcde      | ace         | 3       | ace         |
| abc        | abc         | 3       | abc         |
| abc        | def         | 0       | (无)        |
| oxcpqrsvwf | shmtulqrypy | 2       | qr          |
| bsbininm   | jmjkbkjkv   | 1       | b 或 j 或 k |
